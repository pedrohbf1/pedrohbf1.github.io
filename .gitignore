# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Playwright
/test-results/
/playwright-report/
/blob-report/
/playwright/.cache/
# tests/baseline NAO entra aqui: os PNGs de referencia SAO versionados de proposito
# (prova de antes/depois da otimizacao de imagem — exigencia da Sentinela).
# O que e descartavel ja esta coberto por /test-results/ e /playwright-report/.

# Orquestracao local dos agentes (Maestri) — nao e codigo do portfolio
/.maestri/

# Relatorios de performance / Lighthouse
lighthouse*.json
lighthouse*.html
*.report.html
/.lighthouseci/
# Saida experimental do optimize-images (build real usa --out public)
public-otim/
.manifesto-public-otim.json
